# QuasiMesh Network

Android-приложение для создания децентрализованной quasi-mesh сети без сервера. Телефоны пользователей автоматически подключаются друг к другу, передают интернет, строят карту сети и балансируют нагрузку.

## 🚀 Технические характеристики

- **Минимальная версия Android**: API 26 (Android 8.0)
- **Целевая версия Android**: API 34 (Android 14)
- **Язык**: Kotlin
- **UI Framework**: Jetpack Compose
- **Архитектура**: Чистая архитектура с разделением на managers, services, ui, utils

## 📋 Компоненты

### Основные модули:

1. **NodeManager** - Управление Wi-Fi Direct и Hotspot для подключения к другим узлам
2. **MeshService** - Foreground Service для работы mesh-сети
3. **RoutingEngine** - Алгоритм маршрутизации BFS для построения маршрутов
4. **TrafficLimiter** - Ограничение скорости и приоритизация трафика
5. **HttpServer** - Локальный HTTP сервер для обмена данными между узлами
6. **TopologyMap** - UI компонент для визуализации карты сети

## 🔧 Установка и запуск

### Требования:

- Android Studio Hedgehog или новее
- JDK 8 или выше
- Android SDK с API 26+

### Шаги установки:

1. Клонируйте репозиторий или откройте проект в Android Studio
2. Откройте проект в Android Studio
3. Дождитесь синхронизации Gradle
4. Подключите Android устройство или запустите эмулятор
5. Нажмите Run (Shift+F10) или выберите Run > Run 'app'

### Разрешения:

Приложение запрашивает следующие разрешения:
- `INTERNET` - для сетевых операций
- `ACCESS_WIFI_STATE` / `CHANGE_WIFI_STATE` - для управления Wi-Fi
- `ACCESS_NETWORK_STATE` / `CHANGE_NETWORK_STATE` - для проверки состояния сети
- `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` - требуется для Wi-Fi Direct
- `FOREGROUND_SERVICE` - для работы фонового сервиса
- `POST_NOTIFICATIONS` - для уведомлений

## 📱 Использование

1. Запустите приложение
2. Нажмите кнопку "Участвовать в сети"
3. Приложение начнет поиск соседних узлов
4. После подключения вы увидите:
   - Статус узла
   - Текущий маршрут до интернета
   - Скорость передачи данных
   - Загрузку узла
5. Нажмите "Карта сети" для визуализации топологии

## 🌐 Протокол обмена

### GET /status
Возвращает статус узла в формате JSON:
```json
{
  "node_id": "string",
  "neighbors": ["node1", "node2"],
  "has_internet": true,
  "signal": -55,
  "load": 0.32,
  "timestamp": 123123123
}
```

### POST /routing
Принимает маршрутную таблицу от другого узла:
```json
{
  "node_id": "string",
  "routes": {
    "target_node": "next_hop"
  },
  "distances": {
    "target_node": 2
  },
  "timestamp": 123123123
}
```

## 🏗️ Структура проекта

```
app/
├── src/main/
│   ├── java/com/quasimesh/network/
│   │   ├── NodeManager.kt          # Управление узлами
│   │   ├── MeshService.kt          # Foreground Service
│   │   ├── RoutingEngine.kt        # Алгоритм маршрутизации
│   │   ├── TrafficLimiter.kt       # Ограничение трафика
│   │   ├── HttpServer.kt           # HTTP сервер
│   │   ├── models/
│   │   │   ├── NodeStatus.kt       # Модель статуса узла
│   │   │   └── RoutingTable.kt     # Модель маршрутной таблицы
│   │   ├── ui/
│   │   │   ├── MainActivity.kt     # Главная Activity
│   │   │   ├── MainScreen.kt       # Главный экран
│   │   │   ├── MapScreen.kt        # Экран карты сети
│   │   │   └── theme/
│   │   │       └── Theme.kt        # Тема приложения
│   │   └── utils/
│   │       └── GraphUtils.kt       # Утилиты для графа
│   ├── res/
│   │   ├── values/
│   │   │   ├── strings.xml         # Строковые ресурсы
│   │   │   ├── colors.xml          # Цвета
│   │   │   └── themes.xml          # Темы
│   └── AndroidManifest.xml
├── build.gradle
└── proguard-rules.pro
```

## 🔍 Алгоритм маршрутизации

Приложение использует алгоритм BFS (Breadth-First Search) для построения оптимальных маршрутов:

1. Каждый узел собирает информацию о соседях
2. Строится граф сети
3. BFS находит кратчайший путь до узла с интернетом
4. Маршрутные таблицы обновляются периодически
5. При изменении топологии маршруты пересчитываются

## ⚙️ Настройки

- **HTTP порт**: 8080 (можно изменить в `MeshService.HTTP_PORT`)
- **Интервал ping**: 5 секунд
- **Интервал обновления топологии**: 10 секунд
- **Максимальная скорость**: 10 Мбит/с (можно изменить в `TrafficLimiter`)

## 🐛 Известные ограничения

- В эмуляторе Wi-Fi Direct может работать некорректно
- Для полноценной работы требуется реальное устройство
- IP адреса соседей определяются автоматически (в демо-версии используется localhost)
- В реальном приложении требуется дополнительная логика для определения IP адресов в mesh-сети

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 👨‍💻 Разработка

Для разработки и тестирования рекомендуется использовать несколько физических Android устройств с поддержкой Wi-Fi Direct.

---

**Примечание**: Это демонстрационная версия приложения. Для production использования требуется дополнительная доработка безопасности, обработки ошибок и оптимизации производительности.


