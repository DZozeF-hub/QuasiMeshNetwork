# 🚀 Быстрый старт - Получить готовый APK

## Способ 1: GitHub Actions (РЕКОМЕНДУЕТСЯ) ⭐

**Самый простой способ получить готовый APK:**

1. **Загрузите проект на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   git push -u origin main
   ```

2. **GitHub автоматически соберет APK:**
   - Перейдите в ваш репозиторий на GitHub
   - Откройте вкладку **Actions**
   - Дождитесь завершения сборки (обычно 5-10 минут)
   - Нажмите на завершенный workflow
   - В разделе **Artifacts** скачайте `app-release-apk`

3. **Готово!** У вас есть готовый APK файл! 📱

---

## Способ 2: Локальная сборка через Android Studio

1. Откройте проект в **Android Studio**
2. Дождитесь синхронизации Gradle
3. Выберите: **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
4. APK будет в: `app/build/outputs/apk/release/app-release.apk`

---

## Способ 3: Командная строка (если установлен Android SDK)

```bash
# Windows
gradlew.bat assembleRelease

# Linux/Mac  
./gradlew assembleRelease
```

APK будет в: `app/build/outputs/apk/release/app-release.apk`

---

## ⚠️ Важно

- **GitHub Actions** - самый надежный способ, работает автоматически
- Для локальной сборки нужен **Android SDK**
- APK подписан debug ключом (для production нужен release keystore)

---

## 📱 Установка APK

После получения APK:

1. Скопируйте файл на Android устройство
2. Откройте файл на устройстве
3. Разрешите установку из неизвестных источников (если нужно)
4. Установите приложение

Или через ADB:
```bash
adb install app-release.apk
```

---

**Рекомендация: Используйте GitHub Actions - это самый простой и надежный способ!** 🎯

